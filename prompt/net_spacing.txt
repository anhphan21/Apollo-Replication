# Task 1
- Target: check the logic of the code, complete the module and fix Python interface
- The variable `pin_dir` has the same logic to store the value as the `pos` variable 
    - store the value x 1st and y later with size is num_node*2
- The variable `node_num_ports` has the same logic to store the value as the `pos` variable
    - each node has 4 sides (0: left, 1: right, 2: down/lower, 3: up/upper) corresponding to the integer number
    - to get the number of ports in left side of the node, we can access by node_num_ports[pin2node_map[pin_idx] + pin_side[pin_idx]]
- If the pin is the port of the module (cannot find the idx from pin2node_map), then use 1 as the value of the node_num_ports.
- In the module `net_spacing`, fix the python interface to match with the function defined in source file `src/net_spacing.cpp`.

# Task 2
- Target: check the crossing module in the `net_spacing` model
- write the python test module to test the number of crossing via the net_spacing module.
- can create the new function in the `net_spacing` to parse the number of crossing to check

# Task 3
- Target: create python test code to verify the module.
- The test code will consider a 2-pin net with 2 nodes
    - The 1st pin of the net is the fixed pin with the location is (0,0) with the `pin_dir` is (1, 0)
    - The 2nd pin is the movable pin with `pin_dir` is (-1, 0)
    - The fixed pin belongs to the node with 2 ports are in the same side as that fixed pin
    - The movable pin belongs to the node with 3 ports are in the same side as that pin
    - value of the crossing size is 5
    - value of the bend radii is 5

- The position of the movable node is sweep from -40 to 40